/**
 * Send email token
 * What should it do:
 * WIP
 * @param {string} email
 * @param {string} token
 */
export function sendMailToken(email, token) {
  console.log(`Sending email to ${email} with token: ${token}`)
  return true
}
